{{ form_start(contactForm, {'attr': {'class': 'w-full mx-auto my-5'}}) }}

<div class="flex flex-col gap-4">
    <div class="flex flex-col md:flex-row justify-between gap-4">
        <div class="flex flex-1 flex-col p-4">
            {{ form_label(contactForm.prospect_name) }}
            {{ form_widget(contactForm.prospect_name) }}
            <div class="form-error" role="alert">
                {{ form_errors(contactForm.prospect_name) }}
            </div>
        </div>
        <div class="flex flex-1  flex-col p-4">
            {{ form_label(contactForm.prospect_mail) }}
            {{ form_widget(contactForm.prospect_mail) }}
            <div class="form-error" role="alert">
                {{ form_errors(contactForm.prospect_mail) }}
            </div>
        </div>
    </div>

    <div class="flex flex-col bg-transparent p-4">
        {{ form_label(contactForm.prospect_message) }}
        {{ form_widget(contactForm.prospect_message) }}
        <div class="form-error" role="alert">
            {{ form_errors(contactForm.prospect_message) }}
        </div>
    </div>

    <div class="flex justify-center mt-5">
        <button
            class="relative w-1/2 inline-flex justify-center items-center gap-2 py-3 xl:py-4 px-4 uppercase rounded-sm border border-rostBlue text-rostBlue hover:text-white  duration-300 transition-all group overflow-hidden">
            <div class="text-xs lg:text-sm 2xl:text-lg font-bold leading-none z-10">
                Envoyer <span>&nbsp; &#8594;</span>
            </div>
            <div
                class="absolute inset-0 bg-rostBlue transform -translate-x-[110%] group-hover:translate-x-0 transition-transform duration-500 ease-in-out">
            </div>
        </button>
    </div>

</div>

{{ form_end(contactForm) }}